language: java
dist: trusty

addons:
  sonarcloud:
    organization: "dylanben-github"

script:
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.organization=dylanben-github -Dsonar.host.url=https://sonarcloud.io -Dsonar.projectKey=com.ipi.java.340:master -Dsonar.branch=master
